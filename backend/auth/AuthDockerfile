FROM openjdk:11-jdk-slim as builder
COPY ./re-verse/backend/auth .
# TODO: application.properties 복사
RUN chmod +x ./gradlew
RUN ./gradlew bootJAR

FROM openjdk:11-jdk-slim
COPY --from=builder build/libs/*.jar app.jar
EXPOSE 8080
ENTRYPOINT ["java", "-jar", "/app.jar"]
