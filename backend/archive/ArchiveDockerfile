FROM openjdk:11-jdk-alpine as builder
COPY ./re-verse/backend/archive .
# TODO: application.properties 복사
RUN chmod +x ./gradlew
RUN ./gradlew bootJAR

FROM openjdk:11-jdk-alpine
COPY --from=builder build/libs/*.jar app.jar
EXPOSE 8081
ENTRYPOINT ["java", "-jar", "/app.jar"]